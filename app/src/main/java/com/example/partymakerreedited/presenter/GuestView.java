package com.example.partymakerreedited.presenter;

import com.example.partymakerreedited.models.Event;

public interface GuestView {
    void buildList(Event event);
    void buildMain(Event event);
}
